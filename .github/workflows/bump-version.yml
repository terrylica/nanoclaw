name: Bump version (DISABLED — superseded by semantic-release)

# This workflow is disabled. Version bumping is now handled by
# semantic-release via `mise run release:full` (local execution).
# See .releaserc.yml for configuration.
#
# Original trigger: push to main touching src/** or container/**
# Original action: npm version patch + commit + push

on:
  workflow_dispatch: {}  # manual-only, effectively disabled

jobs:
  bump-version:
    if: false  # never runs
    runs-on: ubuntu-latest
    steps:
      - run: echo "Superseded by semantic-release"
